import styled from 'styled-components';
import { background2, color3, fontSizeS } from '../Shared/Styles';

export default styled.select`
  border: 0;
  color: ${color3};
  float: right;
  margin: 3px 6px;
  ${background2}
  ${fontSizeS}
`;
